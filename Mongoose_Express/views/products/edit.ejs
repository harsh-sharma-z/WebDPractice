<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
</head>
<body>
    <h1>Edit <%= editProduct.name %></h1>
    <form action="/products/<%= editProduct._id  %>?_method=PUT" method="POST">
        <label for="name">Edit the name of the product:</label>
        <input type="text" name="name" placeholder="Enter name" id="name" value="<%= editProduct.name %>">

        <label for="price">Edit the price:</label>
        <input type="number" step="any" name="price" placeholder="Enter price" id="price" value="<%= editProduct.price %>">

        <label for="category">Choose the category:</label>
        <select name="category" id="category" value="<%= editProduct.category %>">
            <% for(let category of categories) {%>
                <option value="<%= category %>" <%= editProduct.category === category ? 'selected' : ''%>>
                    <%= category %>
                </option>
            <% } %>
        </select>
        <button>Submit</button>
    </form>
    <a href="/products/<%= editProduct._id %>">
        <button>Back</button>
    </a>
</body>
</html>