<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add a product</title>
</head>
<body>
    <h1><%= farm.name %></h1>
    <h3>Add a product:</h3>
    <form action="/farms/<%= farm._id %>/products" method="POST">
        <label for="name">Enter the name of the product:</label>
        <input type="text" name="name" placeholder="Enter name" id="name">

        <label for="price">Enter the price:</label>
        <input type="number" name="price" placeholder="Enter price" id="price" step="any">

        <label for="category">Choose the category:</label>
        <select name="category" id="category">
            <% for(let category of categories) {%>
                <option value="<%= category %>"><%= category %></option>
            <% } %>
        </select>
        <button>Submit</button>
    </form>
    
    <br>
    <a href="/farms/<%= farm._id %>">
        <button>Back to farm</button>
    </a>

    
</body>
</html>